# Static "about" page: what the site does, where the data comes from and how
# the numbers are built. No data to load — the view is plain copy.
class AboutController < ApplicationController
  def show
  end
end
